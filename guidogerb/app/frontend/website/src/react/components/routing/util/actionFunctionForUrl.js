/** @typedef {import('design-system-header').EventAction} EventAction */

/**
 * Creates an action function for a URL
 * @param {string} url - The URL to navigate to
 * @returns {Function} The action function
 */
export function actionFunctionForUrl(url) {
  return () => {};
}
